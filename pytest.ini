[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --junitxml=test-results.xml
markers =
    unit: Unit tests
    uat: User Acceptance Tests (Selenium)
    performance: Performance tests (Locust)
